* make console scrollable
* make maybe something fork like

* probably don't have to run other processes only kernel procs

*keyboard mapping/interrupts
--Setup GDT (global description table)
--setup IDT (interrupt description table)
--remap PIC
--write interrupt handlers (check this not sure about how to write them to specific location)
--map keyboard

*or just handling interrupts for that matter

*driver for generic network card
...
